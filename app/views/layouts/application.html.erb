<!DOCTYPE html>
<html>
<head>
  <title>PEDOBOOK</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

<body class="container">
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
	<div class =  "container-fluid">
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
		<a class="navbar-brand" href="#">P E D O B O O K</a>
		<ul class="nav navbar-nav">
	        <li><%= link_to "Discussion Board", topics_path %></li>
      	</ul>
      	<ul class="nav navbar-nav navbar-right">
      	<% if !current_user %>
			<li><%= link_to 'Sign Up', new_user_path %></li>
			<li><%= link_to 'Login', new_sessions_path %></li>
			 <% else %>
			 <li class="navbar-text"> Hi  <%= user_name %>! </li>
			 <li><%= link_to "Sign Out", sessions_path, :method => :delete %></li>
		<% end %>
		<% if current_user %>
	      	<li class="dropdown">
		          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span> <b class="caret"></b></a>
		      	 <ul class="dropdown-menu">
		      	 	<% if profile_id %>
		           <li> <%= link_to 'Profile', profile_path(profile_id) %> </li>
		           <% else %>
		           		<li><%= link_to 'Make a profile', new_profile_path %> </li>
		           <% end %>
		          </ul>
	          </li>
		<% end %>
          </ul>
		</div>
	</div>
</nav>
<div class="container-fluid">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  </div>
  <%= yield %>
</body>
</html>
